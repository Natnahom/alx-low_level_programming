#include <stdio.h>
/**
*main - prints a-z
*
*Description: using main function
*Return: 0 (correct)
*/
int main(void)
{
int ch;

for (ch = 'a'; ch <= 'z'; ch++)
putchar(ch);
putchar(ch);
return (0);
}
